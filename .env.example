# Amazon Price Monitor Configuration

# Required: Google Sheets service account credentials (JSON format)
# Get these from Google Cloud Console -> Service Accounts
GOOGLE_SHEETS_CREDENTIALS='{"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}'

# Required: Google Sheet URL or ID containing ASINs
# Example: https://docs.google.com/spreadsheets/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit
GOOGLE_SHEET_URL="your_google_sheet_url_here"

# Optional: Name of the worksheet (defaults to first sheet)
WORKSHEET_NAME="Sheet1"

# Optional: Column containing ASINs (defaults to 'A')
ASIN_COLUMN="A"

# Optional: Proxy server (format: http://user:pass@host:port)
# PROXY="http://username:password@proxy.example.com:8080"

# Optional: Run browser in headless mode (defaults to True)
HEADLESS="True"

# Optional: Delay range between requests in seconds
MIN_DELAY="3"
MAX_DELAY="8"